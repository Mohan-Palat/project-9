<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies</title>
</head>
<body class="showMovie">
    <h1>Movies</h1>
    <% if (currentUser) { %>
        <a href="/movies/new" ><h5>Add New Movie</h5></a>
        <br>
    <% } %>

    <h2><%= movie.title %></h2>
    <h3><%= movie.description %></h3>

    <% if (currentUser) { %>
        <div class="movieButtonControlList">
            <form action="/movies/<%=movie._id%>/edit?_method=GET" method="POST">
                <button>EDIT MOVIE ON ALL SHOWBOARDS</button>
            </form>
            <br>
            <form action="/movies/<%=movie._id%>?_method=DELETE" method="POST">
                <button>DELETE MOVIE FROM SHOWBOARDS</button>
            </form>
        </div>
    <% } %>

</body>
</html>